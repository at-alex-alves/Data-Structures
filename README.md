# Go - Data Structures

Project implementing data structures to show how storing and organizing data in computers, so it can be used more effectively.

## Prerequisites

- Docker

## Running

Run the following command to execute the project:

```
docker-compose up
```
